{
  "name": "@example/minimal",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "tinacms dev -c \"astro dev\"",
    "build": "export NODE_OPTIONS='--max-old-space-size=4096' && (if [ \"$NETLIFY\" = \"true\" ]; then echo 'Skipping Tina build on Netlify'; else tinacms build; fi) && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@tinacms/cli": "^2.3.1",
    "astro": "^6.3.3",
    "tinacms": "^3.8.1"
  }
}
